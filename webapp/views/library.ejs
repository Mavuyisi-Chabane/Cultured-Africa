<!DOCTYPE html>
<html lang="en">
<head>
<%- include('partials/head', { title: 'Cultured Africa — My Library' }) %>
</head>
<body class="min-h-screen">
<%- include('partials/header') %>

<main class="max-w-7xl mx-auto px-6 py-8 relative">
    <div class="absolute inset-0 pattern-overlay pointer-events-none"></div>

    <div class="mb-8">
        <h1 class="text-2xl md:text-3xl font-bold text-[#1a1a2e] mb-1">My Library</h1>
        <p class="text-[#8a7f73] text-sm">Films you own — purchased titles and free content, ready to watch anytime.</p>
    </div>

    <% if (films.length === 0) { %>
        <div class="bg-white rounded-[24px] border border-[#e8e0d5] p-12 text-center">
            <i class="fa-solid fa-clapperboard text-3xl text-[#c9a84c] mb-4"></i>
            <p class="text-[#2d2d2d] font-medium mb-2">Your library is empty.</p>
            <p class="text-[#8a7f73] text-sm mb-6">Purchase a film or watch something free to add it here.</p>
            <a href="/" class="inline-block bg-[#c9a84c] hover:bg-[#b8973d] text-white px-6 py-3 rounded-xl font-bold text-sm shadow-md shadow-[#c9a84c]/20 transition-all">Browse Films</a>
        </div>
    <% } else { %>
        <div id="library-grid" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
            <% films.forEach(film => { %>
                <a href="/film/<%= film.id %>" class="bg-white rounded-[16px] shadow-sm border border-[#e8e0d5] overflow-hidden group hover:shadow-xl transition-all duration-300">
                    <div class="h-56 overflow-hidden relative">
                        <img class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500" src="<%= film.thumbnailUrl %>" alt="<%= film.title %>" />
                        <div class="absolute top-4 right-4 bg-white/90 backdrop-blur px-2 py-1 rounded text-[10px] font-bold text-[#1a1a2e] uppercase tracking-widest">
                            <%= film.purchasedAt ? 'Purchased' : 'Free' %>
                        </div>
                    </div>
                    <div class="p-6">
                        <h3 class="text-xl font-bold text-[#1a1a2e] mb-1"><%= film.title %></h3>
                        <p class="text-[#8a7f73] text-xs font-medium mb-4 italic"><%= film.culture %> Heritage · <%= film.genre %></p>

                        <div class="flex items-center gap-1 mb-4 text-[#c9a84c]">
                            <% const full = Math.floor(film.rating); const half = film.rating - full >= 0.5; %>
                            <% for (let i = 0; i < 5; i++) { %>
                                <% if (i < full) { %>
                                    <i class="fa-solid fa-star text-xs"></i>
                                <% } else if (i === full && half) { %>
                                    <i class="fa-solid fa-star-half-stroke text-xs"></i>
                                <% } else { %>
                                    <i class="fa-regular fa-star text-xs"></i>
                                <% } %>
                            <% } %>
                            <span class="text-[#8a7f73] text-xs ml-1 font-normal">(<%= film.rating.toFixed(1) %>)</span>
                        </div>

                        <div class="flex items-center justify-between mt-6 pt-4 border-t border-[#f5f0e8]">
                            <span class="text-[#8a7f73] text-xs">
                                <% if (film.purchasedAt) { %>
                                    Bought <%= film.purchasedAt.toLocaleDateString() %>
                                <% } else { %>
                                    Free to watch
                                <% } %>
                            </span>
                            <span class="bg-[#c9a84c] group-hover:bg-[#b8973d] text-white px-6 py-2.5 rounded-xl font-bold text-sm shadow-md shadow-[#c9a84c]/20 transition-all">Watch</span>
                        </div>
                    </div>
                </a>
            <% }) %>
        </div>
    <% } %>

    <%- include('partials/footer') %>
</main>
</body>
</html>
